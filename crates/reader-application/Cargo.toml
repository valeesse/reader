[package]
name = "reader-application"
version = "0.1.1"
edition = "2024"
rust-version = "1.96"

[dependencies]
reader-contracts = { path = "../reader-contracts" }
reader-core = { path = "../reader-core" }

[dev-dependencies]
tempfile = "3"
